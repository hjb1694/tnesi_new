{
  "name": "tnesi",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "sass:build": "sass --style=compressed ./src/scss/main.scss ./assets/style.css",
    "sass:watch": "sass --watch --style=compressed ./src/scss/main.scss ./assets/style.css",
    "terser:build:header": "terser ./src/js/header_toggle.js -c --output ./assets/js/header_toggle.min.js",
    "terser:build:all": "concurrently 'npm run terser:build:header'"
  },
  "keywords": [],
  "author": "Hayden Bradfield",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^10.0.0",
    "sass": "^1.93.2",
    "terser": "^5.48.0"
  },
  "dependencies": {
  }
}
